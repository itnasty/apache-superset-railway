# Database drivers
pymysql==1.1.1
psycopg2-binary==2.9.9
mysqlclient==2.2.4

# Additional dependencies for Railway deployment
redis==4.6.0

# CORS support
flask-cors==4.0.0

# Production server dependencies
# Flask is already included in apache/superset image, don't override
# IMPORTANT: Superset 4.0.2 requires gunicorn <22.0
gunicorn==21.2.0
gevent==24.11.1  # For async/streaming support
